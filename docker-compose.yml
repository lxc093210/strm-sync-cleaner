services:
  strm-sync-cleaner:
    build: .
    container_name: strm-sync-cleaner
    volumes:
      - /volume1/1000/docker/AppData/strm:/raw
      - /volume1/1000/lxcemby/video:/sorted
    ports:
      - "8000:8000"     # Web UI
    restart: unless-stopped
